pipeline {
        agent any
	
	stages {
		stage("setup python"){
                        echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
			dir(/home/idimmusix/loveletterwriter.api) {
                        sh "pwd"
	                }
	        }
        }
}
