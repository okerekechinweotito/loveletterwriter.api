name: Deploy update/dev CI

on:
  push:
    branches: [ "update/dev" ]

jobs:
  build:

    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: Go to folder
      run: |
        cd /var/www/loveletterwriter.api
        source env/bin/activate
        git switch update/dev
        git pull
        pip install -r "requirements.txt"
    - name: restart server
      run: sudo systemctl restart hng
